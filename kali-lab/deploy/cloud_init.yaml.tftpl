#cloud-config
hostname: ${hostname}
manage_etc_hosts: true
users:
  - name: kali
    groups: [sudo]
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    ssh_authorized_keys:
      - ${ssh_pubkey}
ssh_pwauth: true
package_update: false
